import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plimeor.flint',
  appName: 'Flint',
  webDir: './dist/public',
  bundledWebRuntime: false
};

export default config;
